import { TitulosDirective } from './titulos.directive';

describe('TitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new TitulosDirective();
    expect(directive).toBeTruthy();
  });
});
